# web/ — Rydafirst (Next.js)

Responsive web client. Builds against `../contracts/openapi.yaml` (generate a typed client) and
imports design tokens from `../contracts/design-tokens.ts` (copied to `src/design-tokens.ts`).
Ships first; mobile is built alongside against the same contract.
